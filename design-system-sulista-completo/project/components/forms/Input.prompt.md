Campo de texto com label, hint e estado de erro.
```jsx
<Input label="Placa do veículo" placeholder="ABC-1D23" mono hint="Formato Mercosul" />
```
`mono` para códigos; `prefix` para unidades; `error` substitui o hint.
